# Core
pandas
numpy
pyarrow

# NLP (skill extraction) - spaCy instead of Spark NLP: same NER concepts,
# no JVM/licensing overhead, runs reliably on a laptop for a solo 4-month build.
spacy

# Distributed processing (local mode - no EMR/cluster cost)
pyspark
delta-spark

# ML
scikit-learn
xgboost
joblib

# Dashboard
streamlit
plotly

# Orchestration (stretch goal, Month 2+)
# apache-airflow  # install separately via Docker - heavy dependency tree

# Streaming (stretch goal, Month 4) - see infra/kafka/docker-compose.yml
kafka-python

# Skill search (stretch goal) - see infra/elasticsearch/docker-compose.yml
elasticsearch

# AWS (Month 4 - only when we actually push to S3)
boto3

# Dev
pytest
python-dotenv
